interface:
  display_name: "OWASP Code Review"
  short_description: "Autonomous, local-first OWASP security review"
  default_prompt: "Use $owasp-codex-skill as an independent read-only reviewer. Audit this codebase, safely use only approved local open-source tools when useful, validate their candidates manually, and produce an evidence-backed OWASP report."
